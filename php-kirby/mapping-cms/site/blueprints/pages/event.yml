name: event

sections:
  info:
    type: fields
    fields:
      name:
        label: 
        type: text
      artists:
        label: involved artists
        type: multiselect
        options: query
        query: site.find("artists").children
      text:
        type: textarea
        size: medium
    
  gallery:
    type: files
    layout: cards
    image:
      ratio: 1/1 